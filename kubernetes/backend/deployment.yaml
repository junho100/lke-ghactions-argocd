apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-service
  labels:
    app: test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test
  template:
    metadata:
      labels:
        app: test
    spec:
      containers:
        - name: test-service
          image: bemodesty306/backend:lastest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
